### This repo is to setup uniswap on local ethereum networ

### setup

- `ganache-cli` - this will run your local ethereum node
- `bash setup.sh` - this will run migrations and setup uniswap local setup
